<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textColorLink">@color/colorLink</item>
        <item name="android:fontFamily">@font/oxygen</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <!--<item name="android:editTextBackground">@color/colorTextView</item>-->
        <item name="android:colorButtonNormal">@color/colorButton</item>
        <item name="android:textColorHint">@color/colorHint</item>
        <item name="android:editTextBackground">@drawable/templatetxt</item>
        <item name="android:textSize">14dp</item>
    </style>

    <style name="Button" >
        <item name="android:background">@drawable/templatebtn</item>
        <item name="android:textColor">@color/colorTextButton</item>
    </style>

</resources>
